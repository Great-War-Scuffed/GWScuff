# France decisions - parliament minigame removed for multiplayer
# The old national assembly GUI and timed vote decisions have been stripped.
# Only non-minigame utility decisions are kept below.

debug_decisions = {

	FRA_reload_gui = {
		icon = GFX_decision_SOV_place_hq
		allowed = {
			original_tag = FRA
		}
		available = {
			is_ai = no
		}
		visible = {
			is_debug = yes
		}
		remove_effect = {
			reload_parliament_gui = yes
		}
	}
}

FRA_colonial_decisions = {

	FRA_hide_IND_tree = {
		icon = GFX_decision_generic_speech
		allowed = { original_tag = FRA }
		visible = {
			has_subject = IND
			NOT = { has_country_flag = hide_indochinese_tree }
		}
		complete_effect = {
			set_country_flag = hide_indochinese_tree
			mark_focus_tree_layout_dirty = yes
		}
		fire_only_once = no
		cost = 0
	}

	FRA_show_IND_tree = {
		icon = GFX_decision_generic_speech
		allowed = { original_tag = FRA }
		visible = {
			has_subject = IND
			has_country_flag = hide_indochinese_tree
		}
		complete_effect = {
			clr_country_flag = hide_indochinese_tree
			mark_focus_tree_layout_dirty = yes
		}
		fire_only_once = no
		cost = 0
	}

	FRA_press_african_tribes_to_abolish_slavery = {
		icon = GFX_decision_generic_independence
		fire_only_once = yes
		cancel_if_not_visible = no
		days_mission_timeout = 750
		activation = {
			tag = FRA
			OR = {
				556 = {
					is_controlled_by_ROOT_or_subject = yes
					has_dynamic_modifier = { modifier = generic_tribal_slavery }
				}
				1162 = {
					is_controlled_by_ROOT_or_subject = yes
					has_dynamic_modifier = { modifier = generic_tribal_slavery }
				}
			}
		}
		available = {
			OR = {
				has_completed_focus = FRA_ww1_press_abolition_of_slavery_in_africa
				AND = {
					556 = {
						OR = {
							is_controlled_by_ROOT_or_subject = no
							NOT = { has_dynamic_modifier = { modifier = generic_tribal_slavery } }
						}
					}
					1162 = {
						OR = {
							is_controlled_by_ROOT_or_subject = no
							NOT = { has_dynamic_modifier = { modifier = generic_tribal_slavery } }
						}
					}
				}
			}
		}
		timeout_effect = {
			country_event = { id = ww1_france.492 days = 2 }
			every_state = {
				limit = {
					is_controlled_by_ROOT_or_subject = yes
					has_dynamic_modifier = { modifier = generic_tribal_slavery }
				}
				add_resistance = 25
				add_compliance = -50
			}
			add_stability = -0.1
			add_popularity = { ideology = ROOT popularity = -0.1 }
		}
		visible = { tag = FRA }
		is_good = no
	}
}
